import { StyleSheet, Text, View } from "react-native";
import React from "react";

const AddReviewModal = () => {
  return (
    <View>
      <Text>AddReviewModal</Text>
    </View>
  );
};

export default AddReviewModal;

const styles = StyleSheet.create({});
